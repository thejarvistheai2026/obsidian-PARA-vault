# Daily Notes — 2026-03-13

## Major System Reconstruction

Today was a massive reconstruction day. Restored and fixed nearly all systems.

### ✅ What We Fixed

1. **Context Overflow Crashes** — Edited AGENTS.md to stop reading daily memory files at startup. This was causing session crashes due to excessive context.

2. **Tools Config** — Removed "coding" profile (contained missing `apply_patch` tool) and switched to explicit allowlist.

3. **iMessage** — Fully operational! Required:
   - Installing `imsg` via `brew install steipete/tap/imsg`
   - Adding Full Disk Access for: `imsg`, `Node`, and `Terminal`
   - Updating `openclaw.json` with correct `cliPath` and `dbPath`
   - Gateway restart

4. **Discord (AvaBot)** — Restored bot token from backup. Bot now online and responding.

5. **Discord Observer Bot** — Updated with separate token. Running PID 63070, capturing messages 24/7.

6. **Web Search (Brave)** — Restored API key. Now functional.

7. **Daily Status Report** — Built entire system from scratch:
   - Script: `/scripts/daily-status-report.sh` — checks all systems, sends via iMessage
   - LaunchAgent: `ai.thejarvist.openclaw.daily-status` — runs at 06:30 AM daily
   - First report scheduled for tomorrow morning

8. **Newsletter System** — Fixed:
   - Created missing symlinks in `cron/` directory (ingest-gmail.sh, process.sh, digest.sh)
   - Fixed bash syntax (`local` keyword outside functions)
   - Cleared corrupted database records
   - Now ingesting and processing newsletters

9. **Daily Backup** — Confirmed working at 23:00. Git commits local, push needs auth setup.

### 📊 Current Status

All critical systems operational:
- ✅ iMessage (send/receive)
- ✅ Discord (AvaBot + Observer)
- ✅ Web Search
- ✅ Newsletter (Sun/Thu schedule)
- ✅ Daily Status Report (06:30 AM)
- ✅ Daily Backup (23:00)

### 🔧 Remaining

- GitHub push authentication (SSH keys or token needed)

### 💡 Key Learning

Backup scripts need the actual binary paths in Full Disk Access, not symlinks. LaunchAgents run in different permission contexts than Terminal.

---
**Last Updated:** 2026-03-13 19:31 EDT
